type Mutation {
    setCount(input: Int!): Int!
    decrementCount: Int!
    incrementCount: Int!
    login(input: LoginInput!): Boolean!
}

input LoginInput {
    username: String!
    password: String!
}